# Tap Map to Set Destination

Tap or click any location on the Leaflet map to:
- Populate the Destination field with latitude and longitude.
- Add or move the destination marker.
- Show a confirmation message.
- Keep navigation stopped until the user presses **Calculate Route**.

This avoids accidentally starting navigation from a single map tap.
